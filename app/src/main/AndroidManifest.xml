<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

<!-- Internet -->
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

<uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

<application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/Theme.CropCare">

    <meta-data
        android:name="firebase_analytics_collection_enabled"
        android:value="false" />

    <meta-data
        android:name="firebase_crashlytics_collection_enabled"
        android:value="false" />

    <meta-data
        android:name="firebase_performance_collection_enabled"
        android:value="false" />

    <activity
        android:name=".presentation.splash.SplashActivity"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>

    <activity android:name=".presentation.onboarding.OnboardingActivity" android:exported="false" />

    <activity android:name=".presentation.auth.LoginActivity" android:exported="false" />
    <activity android:name=".presentation.auth.SignupActivity" android:exported="false" />

    <activity android:name=".presentation.home.HomeActivity" android:exported="false" />

    <activity android:name=".presentation.scan.ScanActivity" android:exported="false" />
    <activity android:name=".presentation.result.ResultActivity" android:exported="false" />

    <activity android:name=".presentation.news.NewsActivity" android:exported="false" />

    <activity android:name=".presentation.history.HistoryActivity" android:exported="false" />
    <activity android:name=".presentation.history.HistoryDetailActivity" android:exported="false" />

    <activity android:name=".presentation.weather.WeatherActivity" android:exported="false" />

    <activity android:name=".presentation.profile.ProfileActivity" android:exported="false" />

    <activity android:name=".presentation.chatbot.ChatbotActivity" android:exported="false" />

</application>

</manifest>
